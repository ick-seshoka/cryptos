import React from 'react';

import { Footer, LogoText } from './style';
import LogoIcon from '../logoIcon';

const footer = () => (
    <Footer>
        <LogoIcon tiny />
        <LogoText>cryptos</LogoText>
    </Footer>
)

export default footer;
